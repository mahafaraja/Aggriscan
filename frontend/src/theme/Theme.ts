
import { colors } from './Color';
import { spacing } from './Spacing';
import { typography } from './Typography';

export const theme = {
  colors,
  spacing,
  typography,

  radius: {
    input: 12,
    card: 16,
    button: 24,
    screen: 24,
    full: 999,
  },
};
