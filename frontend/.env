# Render Backend Configuration
EXPO_PUBLIC_API_BASE_URL=https://aggriscan.onrender.com

# Note: SMS API keys are handled by the backend, not the frontend
# The frontend only needs to know the API base URL
